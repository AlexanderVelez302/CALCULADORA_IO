"""Tests para solver de Colas (M/M/1).\nValida métricas de teoría de colas.\"\"\"\n\nimport pytest\nfrom solvers.colas import resolver_colas\n\n\nclass TestColas:\n    \"\"\"Tests para solver de colas M/M/1\"\"\"\n    \n    def test_colas_basic(self):\n        \"\"\"Test caso básico: λ=10, μ=12\"\"\"\n        enunciado = \"Tasa de llegada λ = 10 clientes/hora, Tasa servicio μ = 12 clientes/hora\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        # ρ = λ/μ = 10/12 = 0.833 = 83.3%\n        assert \"83.3\" in resultado or \"0.83\" in resultado or \"83\" in resultado\n    \n    def test_colas_ejemplo_libro(self):\n        \"\"\"Test Ejemplo 5.1 Hillier: L≈5, ρ≈83.3%\"\"\"\n        enunciado = \"\"\"Sistema de colas M/M/1\n        Tasa de llegada: λ = 10 clientes por hora\n        Tasa de servicio: μ = 12 clientes por hora\"\"\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        # Debería contener métrica de número promedio en sistema\n        assert \"L\" in resultado or \"5\" in resultado\n    \n    def test_colas_stable_system(self):\n        \"\"\"Test sistema estable: λ < μ\"\"\"\n        enunciado = \"λ = 5, μ = 10\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        # ρ = 0.5 (50%)\n        assert \"0.5\" in resultado or \"50\" in resultado\n    \n    def test_colas_near_saturation(self):\n        \"\"\"Test sistema cerca de saturación: λ ≈ μ\"\"\"\n        enunciado = \"λ = 9.5, μ = 10\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        # ρ ≈ 0.95 (95%), valores grandes\n    \n    def test_colas_decimal_rates(self):\n        \"\"\"Test con tasas decimales\"\"\"\n        enunciado = \"λ = 3.5 clientes/hora, μ = 4.2 clientes/hora\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n    \n    def test_colas_different_unit(self):\n        \"\"\"Test con diferentes unidades de tiempo\"\"\"\n        enunciado = \"λ = 2 clientes/minuto, μ = 3 clientes/minuto\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n\n\nclass TestColasValidation:\n    \"\"\"Tests de validación para colas\"\"\"\n    \n    def test_colas_unstable_system(self):\n        \"\"\"Test sistema inestable: λ >= μ (no solución)\"\"\"\n        enunciado = \"λ = 12, μ = 10\"\n        resultado = resolver_colas(enunciado)\n        \n        # Debe indicar que el sistema no es estable\n        assert resultado is None or \"inestable\" in resultado.lower() or \"unstable\" in resultado.lower()\n    \n    def test_colas_zero_arrival(self):\n        \"\"\"Test con tasa de llegada cero\"\"\"\n        enunciado = \"λ = 0, μ = 10\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None or resultado is None  # Caso degenerado\n    \n    def test_colas_negative_rate(self):\n        \"\"\"Test con tasas negativas (inválido)\"\"\"\n        enunciado = \"λ = -5, μ = 10\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is None or \"error\" in resultado.lower()\n    \n    def test_colas_invalid_format(self):\n        \"\"\"Test formato inválido\"\"\"\n        enunciado = \"Esto no es un problema de colas\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is None or \"error\" in resultado.lower()\n\n\nclass TestColasOutput:\n    \"\"\"Tests de formato de salida para colas\"\"\"\n    \n    def test_colas_output_contains_utilization(self):\n        \"\"\"Test que output contiene factor de utilización\"\"\"\n        enunciado = \"λ = 10, μ = 12\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        assert \"ρ\" in resultado or \"rho\" in resultado or \"utilización\" in resultado\n    \n    def test_colas_output_contains_metrics(self):\n        \"\"\"Test que output contiene principales métricas\"\"\"\n        enunciado = \"λ = 10, μ = 12\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        # Debería mencionar L, Lq, W, Wq\n        assert any(m in resultado for m in [\"L\", \"W\", \"clientes\", \"tiempo\"])\n    \n    def test_colas_output_numeric(self):\n        \"\"\"Test que output es numérico\"\"\"\n        enunciado = \"λ = 10, μ = 12\"\n        resultado = resolver_colas(enunciado)\n        \n        assert resultado is not None\n        assert any(char.isdigit() for char in resultado)\n"

"""Tests para NLP (No-linear Programming).\nValida optimización de beneficio y problemas de almacén.\"\"\"\n\nimport pytest\nfrom solvers.nlp import resolver_nlp_beneficio, resolver_nlp_almacen\n\n\nclass TestNLPBeneficio:\n    \"\"\"Tests para optimización de beneficio\"\"\"\n    \n    def test_nlp_beneficio_simple(self):\n        \"\"\"Test caso simple de maximización de beneficio\"\"\"\n        enunciado = \"\"\"Función de demanda: P = 100 - 0.5Q\n        Costo unitario: c = 20\"\"\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n        # Q* = 80, P* = 60, B* = $3200\n        assert \"80\" in resultado or \"3200\" in resultado\n    \n    def test_nlp_beneficio_ejemplo_libro(self):\n        \"\"\"Test Ejemplo 6.1 Hillier: Q*=2500, B*=$52,500\"\"\"\n        enunciado = \"\"\"Demanda: P = 150 - 0.01Q\n        Costo variable: c = 50\"\"\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n        assert \"2500\" in resultado or \"52500\" in resultado\n    \n    def test_nlp_beneficio_constant_price(self):\n        \"\"\"Test con precio constante\"\"\"\n        enunciado = \"P = 100, c = 20\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n    \n    def test_nlp_beneficio_linear_demand(self):\n        \"\"\"Test con demanda lineal\"\"\"\n        enunciado = \"P = 200 - 2Q, c = 50\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n    \n    def test_nlp_beneficio_decimal_coeff(self):\n        \"\"\"Test con coeficientes decimales\"\"\"\n        enunciado = \"P = 99.5 - 0.25Q, c = 19.99\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n\n\nclass TestNLPAlmacen:\n    \"\"\"Tests para problemas de almacén\"\"\"\n    \n    def test_nlp_almacen_simple(self):\n        \"\"\"Test caso simple de almacén\"\"\"\n        enunciado = \"\"\"Capacidad: xy = 625\n        Costo construcción: 400x\n        Costo almacenamiento: 2y\"\"\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n        # x* ≈ 25.82m, C* ≈ $7745.97\n        assert \"25\" in resultado or \"7745\" in resultado\n    \n    def test_nlp_almacen_ejemplo_libro(self):\n        \"\"\"Test Ejemplo 6.2 Hillier: x*=25.82, C*=7745.97\"\"\"\n        enunciado = \"\"\"Restricción: xy = 625\n        Costo de construcción por metro: 400x\n        Costo de almacenamiento por año: 2y\"\"\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n        assert \"25\" in resultado or \"7745\" in resultado\n    \n    def test_nlp_almacen_different_capacity(self):\n        \"\"\"Test con capacidad diferente\"\"\"\n        enunciado = \"xy = 1000, 500x, 1y\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n    \n    def test_nlp_almacen_decimal_cost(self):\n        \"\"\"Test con costos decimales\"\"\"\n        enunciado = \"xy = 500, 350.5x, 1.5y\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n\n\nclass TestNLPValidation:\n    \"\"\"Tests de validación para NLP\"\"\"\n    \n    def test_nlp_beneficio_invalid_format(self):\n        \"\"\"Test formato inválido para beneficio\"\"\"\n        enunciado = \"Esto no es un problema de beneficio\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is None or \"error\" in resultado.lower()\n    \n    def test_nlp_almacen_invalid_format(self):\n        \"\"\"Test formato inválido para almacén\"\"\"\n        enunciado = \"Esto no es un problema de almacén\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is None or \"error\" in resultado.lower()\n    \n    def test_nlp_beneficio_zero_cost(self):\n        \"\"\"Test con costo cero\"\"\"\n        enunciado = \"P = 100 - Q, c = 0\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n    \n    def test_nlp_almacen_zero_capacity(self):\n        \"\"\"Test con capacidad cero (inválido)\"\"\"\n        enunciado = \"xy = 0, 400x, 2y\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        # Debería indicar error\n        assert resultado is None or \"error\" in resultado.lower()\n\n\nclass TestNLPOutput:\n    \"\"\"Tests de formato de salida para NLP\"\"\"\n    \n    def test_nlp_beneficio_output_quantity(self):\n        \"\"\"Test que output contiene cantidad óptima\"\"\"\n        enunciado = \"P = 100 - Q, c = 20\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n        assert any(char.isdigit() for char in resultado)\n    \n    def test_nlp_beneficio_output_profit(self):\n        \"\"\"Test que output menciona beneficio\"\"\"\n        enunciado = \"P = 100 - Q, c = 20\"\n        resultado = resolver_nlp_beneficio(enunciado)\n        \n        assert resultado is not None\n        assert any(word in resultado.lower() for word in [\"beneficio\", \"profit\", \"ingreso\", \"revenue\"])\n    \n    def test_nlp_almacen_output_dimensions(self):\n        \"\"\"Test que output contiene dimensiones\"\"\"\n        enunciado = \"xy = 625, 400x, 2y\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n        assert any(char.isdigit() for char in resultado)\n    \n    def test_nlp_almacen_output_cost(self):\n        \"\"\"Test que output menciona costo\"\"\"\n        enunciado = \"xy = 625, 400x, 2y\"\n        resultado = resolver_nlp_almacen(enunciado)\n        \n        assert resultado is not None\n        assert \"costo\" in resultado.lower() or \"cost\" in resultado.lower()\n"

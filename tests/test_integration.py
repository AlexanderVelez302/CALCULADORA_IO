"""Tests para módulos de integración (Detector, Router).\nValida flujo completo de clasificación y enrutamiento.\"\"\"\n\nimport pytest\nfrom core.detector_ia import detectar_tipo_problema\nfrom core.router import router\n\n\nclass TestDetector:\n    \"\"\"Tests para detector de tipo de problema\"\"\"\n    \n    def test_detector_pert(self):\n        \"\"\"Test detección de problema PERT\"\"\"\n        enunciado = \"A(3) B(4) C(5), A→B→C\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"pert\"\n    \n    def test_detector_flujo(self):\n        \"\"\"Test detección de problema de flujo\"\"\"\n        enunciado = \"Nodo 1: 2(10) 3(5), Nodo 2: 4(8)\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"flujo\"\n    \n    def test_detector_transporte(self):\n        \"\"\"Test detección de problema de transporte\"\"\"\n        enunciado = \"Oferta: A(50) B(40), Demanda: X(30) Y(60), A-X: 1, A-Y: 2\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"transporte\"\n    \n    def test_detector_eoq(self):\n        \"\"\"Test detección de problema EOQ\"\"\"\n        enunciado = \"Demanda anual = 1000, Costo de pedido = 50, Costo mantenimiento = 0.2\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"eoq\"\n    \n    def test_detector_colas(self):\n        \"\"\"Test detección de problema de colas\"\"\"\n        enunciado = \"Tasa de llegada λ = 10, Tasa de servicio μ = 12\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"colas\"\n    \n    def test_detector_lp(self):\n        \"\"\"Test detección de problema LP\"\"\"\n        enunciado = \"Maximizar: 3x + 2y\\ns.a.\\nx + y <= 4\\nx <= 2\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"lp\"\n    \n    def test_detector_nlp_beneficio(self):\n        \"\"\"Test detección de problema NLP (beneficio)\"\"\"\n        enunciado = \"Demanda P = 100 - Q, Costo c = 20\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        # Podría ser nlp o lp, validar que detec algo\n        assert tipo is not None\n    \n    def test_detector_nlp_almacen(self):\n        \"\"\"Test detección de problema NLP (almacén)\"\"\"\n        enunciado = \"xy = 625, Costo construcción 400x, Almacenamiento 2y\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo is not None\n\n\nclass TestDetectorFallback:\n    \"\"\"Tests para fallback a Groq en detector\"\"\"\n    \n    def test_detector_ambiguous_text(self):\n        \"\"\"Test texto ambiguo (puede usar Groq)\"\"\"\n        enunciado = \"Necesito optimizar la producción de widgets con restricciones de recursos\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        # Debería intentar clasificar (puede ser lp o nlp)\n        assert tipo is not None or tipo is None  # Aceptar ambos\n    \n    def test_detector_empty_input(self):\n        \"\"\"Test entrada vacía\"\"\"\n        enunciado = \"\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        # Debería retornar None o desconocido\n        assert tipo is None or tipo == \"desconocido\"\n    \n    def test_detector_spanish_keywords(self):\n        \"\"\"Test con palabras clave en español\"\"\"\n        enunciado = \"El máximo flujo desde la fuente al sumidero es:\"\n        tipo = detectar_tipo_problema(enunciado)\n        \n        assert tipo == \"flujo\" or tipo is not None\n\n\nclass TestRouter:\n    \"\"\"Tests para router (enrutamiento a solvers)\"\"\"\n    \n    def test_router_pert(self):\n        \"\"\"Test router con problema PERT\"\"\"\n        enunciado = \"A(3) B(4) C(5), A→B→C\"\n        resultado = router(enunciado)\n        \n        assert resultado is not None\n        assert \"error\" not in resultado.lower()\n    \n    def test_router_transporte(self):\n        \"\"\"Test router con problema transporte\"\"\"\n        enunciado = \"Oferta: A(50), Demanda: X(50), A-X: 1\"\n        resultado = router(enunciado)\n        \n        assert resultado is not None\n        assert \"error\" not in resultado.lower()\n    \n    def test_router_lp(self):\n        \"\"\"Test router con problema LP\"\"\"\n        enunciado = \"Maximizar: x + y\\ns.a.\\nx + y <= 10\\nx <= 5\"\n        resultado = router(enunciado)\n        \n        assert resultado is not None\n    \n    def test_router_unknown_problem(self):\n        \"\"\"Test router con tipo desconocido\"\"\"\n        enunciado = \"Texto completamente irrelevante sin estructura\"\n        resultado = router(enunciado)\n        \n        # Debería manejar gracefully (error o None)\n        assert resultado is None or isinstance(resultado, str)\n    \n    def test_router_error_handling(self):\n        \"\"\"Test que router maneja errores internos\"\"\"\n        enunciado = \"A(abc) B(def), A→B\"  # Duraciones inválidas\n        resultado = router(enunciado)\n        \n        # Debería retornar error o None, no crash\n        assert resultado is None or \"error\" in resultado.lower() or isinstance(resultado, str)\n    \n    def test_router_empty_input(self):\n        \"\"\"Test router con entrada vacía\"\"\"\n        enunciado = \"\"\n        resultado = router(enunciado)\n        \n        assert resultado is None or resultado == \"\"  or \"error\" in resultado.lower()\n\n\nclass TestRouterIntegration:\n    \"\"\"Tests de integración completa detector→router→solver\"\"\"\n    \n    def test_full_pipeline_pert(self):\n        \"\"\"Test pipeline completo PERT\"\"\"\n        enunciado = \"A(2) B(3) C(4), A→B→C\"\n        resultado = router(enunciado)\n        \n        assert resultado is not None\n        assert any(char.isdigit() for char in resultado)\n    \n    def test_full_pipeline_lp(self):\n        \"\"\"Test pipeline completo LP\"\"\"\n        enunciado = \"Maximizar 2x + 3y s.a. x + y <= 5, x >= 0, y >= 0\"\n        resultado = router(enunciado)\n        \n        assert resultado is not None\n    \n    def test_full_pipeline_multiple_runs(self):\n        \"\"\"Test múltiples llamadas a router (estado limpio)\"\"\"\n        enunciados = [\n            \"A(1) B(1), A→B\",\n            \"λ=5, μ=10\",\n            \"max x+y, x+y<=10\"\n        ]\n        \n        for enunciado in enunciados:\n            resultado = router(enunciado)\n            assert resultado is not None\n"
